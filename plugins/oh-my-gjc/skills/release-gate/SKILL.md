---
name: release-gate
description: 3게이트 릴리스 절차를 실행한다 — 검증 게이트(테스트·정적·설치/빌드 repro) → 외부 교차리뷰 게이트(fresh-context 리뷰어 VERDICT, fail-closed) → 인간 승인 게이트(에이전트 자기 승인 절대 금지) → 기계적 발행 + 증거 문서. "릴리스 게이트 / 릴리즈하자 / 배포 게이트 / release gate / 릴리스 검증 절차 / 3게이트" 같은 요청에 활성화.
---

# release-gate — 3게이트 릴리스 절차

목적: "에이전트가 검토 없이 혼자 main에 머지+태그" 사고(oh-my-gjc 2026-07-08, self-merge
4연발)를 구조적으로 차단하는 릴리스 세리머니. 각 게이트는 서로 다른 실패 모드를 막는다 —
**기계가 깨짐을 잡고(G1), 맥락 없는 남이 자기합리화를 잡고(G2), 사람이 책임지고(G3),
전 과정이 증거로 남는다.** 실증: oh-my-gjc v0.15.1·v0.16.0에서 리뷰어가 릴리스 3연속으로
실블로커(수치 과장·표면 누락·기본값 파손 풋건)를 잡아냄.

## Gate 1 — 검증 (기계적, 로컬)

레포에 존재하는 검증 수단을 전부 실행하고 **증거(rc·카운트)를 기록**한다:
- 정적: 매니페스트/설정 파스(JSON/YAML), 버전 필드 일치, `bash -n` 등 문법 검사.
- 단위/통합 테스트: 프로젝트 러너(`bun test` 등) — 실패 0이 전제.
- 재현: 신규 설치/빌드를 **격리 환경**(빈 HOME, 클린 클론)에서 끝까지 — "내 머신에선 됐는데" 차단.
- 검증 불가 표면은 pending-environment로 명시(가짜 증거 금지).

## Gate 2 — 외부 교차리뷰 (fail-closed)

작업자가 자기 결과에 도장 찍는 것을 차단한다. `extragoal` 스킬의 리뷰 레인과 동일 기계를
쓴다(재구현 금지):

1. 릴리스 디프(`git diff <직전태그>..HEAD`) 생성 → **이그레스 전 시크릿 스캔**(발견 시 중단).
2. **fresh-context 리뷰어** 기동: `GJC_NOTIFICATIONS=0 gjc -p --no-session --model <교차/고성능 셀렉터> --tools read,search,find` — 세션 무저장, 읽기 도구만, 작업 맥락 미공유.
3. 디프 + 레포 읽기 권한 + 구체 점검 지시를 주고, 마지막 줄에 정확히
   `VERDICT: APPROVE` 또는 `VERDICT: REQUEST_CHANGES`만 출력하게 한다.
4. **Fail-closed:** verdict 부재/기형/타임아웃/REQUEST_CHANGES ⇒ 발행 불가.
   지적 수정 → 커밋 → **새 fresh 리뷰어로 재서명, 최대 2라운드.** 한도 소진 시 릴리스를
   멈추고 사람에게 보고한다(억지 통과 금지).

## Gate 3 — 인간 승인 (자기 승인 절대 금지 — 약화 불가 계약)

게이트 1·2 증거를 첨부해 운영자 승인을 요청한다(관제탑 큐 또는 세션 직접 지시).
**에이전트가 스스로 승인하는 경로는 없다.** 운영자가 세션에서 명시적으로 릴리스를
지시했다면 그 지시가 곧 승인 — 증거 문서에 지시 사실을 기록한다.

## 통과 후 — 기계적 발행 + 증거 박제

- `dev`→`main` `--no-ff` 머지 → 주석 태그 → 릴리스 발행(GitHub Release 등).
- **증거 문서**를 `docs/verification/<프로젝트>-release-vX.Y.Z-<날짜>.md`로 커밋:
  게이트 1 체크리스트 결과, 게이트 2 전 라운드 verdict·블로커·수정 커밋, 게이트 3 승인 근거.
- 빈도 캡(기본: 문서·패치 번들 1일 1릴리스)을 넘기면 예외 근거를 증거 문서에 기록.
- 롤백은 **fix-forward만**: revert 커밋 → 게이트 1·2 재실행 → 새 패치 릴리스.
  태그/릴리스 삭제·강제 이동 금지(superseded 표기).

## 절대 규칙 (약화 금지)

- 자기 승인 금지 · verdict fail-closed · 재서명 ≤2라운드 · 시크릿 스캔 선행 ·
  증거 없이 발행 금지 · 부분 통과("G1만 통과했으니 일단") 발행 금지.
- 관측 증거는 실제 실행 결과만 기록한다 — 검증 주장과 실행 내역이 일치해야 한다.
